(library
  (name roguelib)
  (modules engine player utils ui world elephant pqueue robot snake spider vague world) 
  (libraries notty.unix))
  
(executable
 (public_name rogue)
 (name main)
 (modules main)
 (libraries notty.unix roguelib))

(env
  (dev
    (flags (:standard -w -32 -w -33 -w -34 -w -37 -w -38))))
